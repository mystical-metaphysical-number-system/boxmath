{
  "name": "boxmath-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "studio"
  ],
  "scripts": {
    "test": "npm run test --workspace=boxmath",
    "dev:studio": "npm run dev --workspace=studio"
  }
}
